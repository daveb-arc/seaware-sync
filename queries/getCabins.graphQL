  query getCabins {
  cabins(params: {}) {   
    
    ship {
      id
      key
      name
    }
    id
    key
    name
    number
    category {
        id
        key
        code
        description
    }     
  }
}
